Rewrite as much of the XML version as possible in XPath—should considerably reduce the impedance-matching between TypeScript and XML types.

Add tests for XML include and paste, in both elements and attributes. Extend tests to cover remaining uncovered lines of code.

Add command line option to specify file extensions assumed to be XML?

Update documentation.

Package as CLI plus module.

New logo for Ruth!
